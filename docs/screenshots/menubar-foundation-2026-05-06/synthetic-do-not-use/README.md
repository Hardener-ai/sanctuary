# Synthetic Menu Bar Images

These PNGs were generated by Codex from a local SwiftUI renderer during M1.
They are useful for preserving visual iteration history, but they are not
real-machine screenshots of the macOS menu bar app.

Use the `*-real.png` files captured with `SCREENSHOT_INSTRUCTIONS.md` as the
source of truth for visual approval.
